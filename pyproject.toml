[project]
name = "sql-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.10"
dependencies = [
    "duckdb>=1.3.1",
    "jupyter>=1.1.1",
    "pandas>=2.3.0",
]
